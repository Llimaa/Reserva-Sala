@model DapperWeb.Models.Professor

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Professor</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.CodIdentificacao)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CodIdentificacao)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Nome)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Nome)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Disciplina)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Disciplina)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

    <div class="form-actions no-color">
        @Html.ActionLink("Editar", "Edit", new { id = Model.ID }, new { @class = "btn btn-primary btn-large" })
        @Html.ActionLink("Voltar", "Index", new { }, new { @class = "btn btn-primary btn-large" })
    </div>
    }
</div>
